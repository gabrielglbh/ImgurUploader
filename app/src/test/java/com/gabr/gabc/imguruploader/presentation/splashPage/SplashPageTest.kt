package com.gabr.gabc.imguruploader.presentation.splashPage

/*import androidx.test.ext.junit.rules.ActivityScenarioRule
import androidx.test.platform.app.InstrumentationRegistry
import com.gabr.gabc.imguruploader.di.AppModule
import com.gabr.gabc.imguruploader.di.FirebaseModule
import com.google.firebase.FirebaseApp
import dagger.hilt.android.testing.HiltAndroidRule
import dagger.hilt.android.testing.HiltAndroidTest
import dagger.hilt.android.testing.HiltTestApplication
import dagger.hilt.android.testing.UninstallModules
import org.junit.Before
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner
import org.robolectric.annotation.Config
import org.robolectric.annotation.LooperMode*/

// https://www.kodeco.com/22152158-testing-with-hilt-tutorial-ui-and-instrumentation-tests?page=1
/*@HiltAndroidTest
@Config(application = HiltTestApplication::class)
@RunWith(RobolectricTestRunner::class)
@LooperMode(LooperMode.Mode.PAUSED)
@UninstallModules(AppModule::class, FirebaseModule::class)*/
class SplashPageTest {
    /*@get:Rule(order = 0)
    var hiltAndroidRule = HiltAndroidRule(this)

    @get:Rule(order = 1)
    var activityScenarioRule: ActivityScenarioRule<SplashPage> =
        ActivityScenarioRule(SplashPage::class.java)

    @Before
    fun setUp() {
        hiltAndroidRule.inject()
        val context = InstrumentationRegistry.getInstrumentation().targetContext
        FirebaseApp.initializeApp(context)
    }

    @Test
    fun shouldNavigateTo_LoginPage() {
        // TODO: Close enough, but not capable of faking out dependencies...
        activityScenarioRule.scenario
    }*/
}